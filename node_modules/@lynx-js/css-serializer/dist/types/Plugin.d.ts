import type * as csstree from 'css-tree';
import type { Location } from './LynxStyleNode.js';
export interface Range {
    start: Location;
    end: Location;
}
export declare enum Severity {
    Error = 0,
    Warning = 1,
    Unused = 2,
    NotSupport = 3,
    Deprecated = 4,
    Experimental = 5
}
export interface ParserError {
    name: string;
    severity: Severity;
    message: string;
    range: Range;
    detail?: string;
}
export interface Plugin {
    name: string;
    phaseStandard?(root: csstree.StyleSheet, context: {
        report: (error: ParserError) => void;
    }): void;
}
