export function getRoot(tree, id) {
    let item = tree.get(id);
    let last = id;
    while (item !== undefined) {
        last = item.id;
        item = tree.get(item.parent);
    }
    return last;
}
export const normalize = (max, min, x) => {
    return (x - min) / (max - min);
};
