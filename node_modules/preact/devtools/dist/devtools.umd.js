(function (global, factory) {
	typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('preact')) :
	typeof define === 'function' && define.amd ? define(['exports', 'preact'], factory) :
	(global = global || self, factory(global.preactDevtools = {}, global.preact));
})(this, (function (exports, preact) {
	function initDevTools() {
	  var globalVar = typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : undefined;
	  if (globalVar !== null && globalVar !== undefined && globalVar.__PREACT_DEVTOOLS__) {
	    globalVar.__PREACT_DEVTOOLS__.attachPreact('10.24.0', preact.options, {
	      Fragment: preact.Fragment,
	      Component: preact.Component
	    });
	  }
	}

	initDevTools();

	/**
	 * Display a custom label for a custom hook for the devtools panel
	 * @type {<T>(value: T, name: string) => T}
	 */
	function addHookName(value, name) {
	  if (preact.options.__a) {
	    preact.options.__a(name);
	  }
	  return value;
	}

	exports.addHookName = addHookName;

}));
//# sourceMappingURL=devtools.umd.js.map
